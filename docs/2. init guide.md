# 개발 환경
  - node v22.14.0
  - pnpm v10.8.0
  - docker v27.5.1

# 초기 세팅
  - sh ./clean.sh

# 프로젝트 환경
  ```
  - pnpm
     └ Modorepo(TurboRepo)
        ├ Host
        │  └ Vite + MF(@module-federation/vite)
        │     └ React(TS)
        │        ├ react router + keepalive-for-react
        │        └ mantine + tailwindcss
        ├ Dashboard
        │  └ Vite + MF(@module-federation/vite)
        │     └ React(TS)
        │        ├ react router + keepalive-for-react
        │        └ mantine + tailwindcss
        └ RCA
           └ Vite + MF(@module-federation/vite)
              └ React(TS)
                 ├ react router + keepalive-for-react
                 └ mantine + tailwindcss
  ```
  * 추가적으로 앱을 추가하고자 한다면 "[5. add-app guide.md](./5.%20add-app%20guide.md)"를 참고